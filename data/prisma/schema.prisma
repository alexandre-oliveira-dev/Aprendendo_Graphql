generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL_WITH_SCHEMA")
}

model Users {
  id   String @id @default(uuid())
  name String
  age  Int
}
